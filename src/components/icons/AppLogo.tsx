"use client";
import type { LucideProps } from 'lucide-react';
import { Files } from 'lucide-react';

const AppLogo = (props: LucideProps) => {
  return <Files {...props} />;
};

export default AppLogo;
